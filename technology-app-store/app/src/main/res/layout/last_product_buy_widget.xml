<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/white"
    android:padding="@dimen/activity_horizontal_margin"
    android:orientation="vertical">

    <TextView
        android:id="@+id/txt_widget_product_price"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/card_title_margin_start"
        android:layout_marginEnd="@dimen/card_title_margin_end"
        android:textColor="@color/colorPrimary"
        android:text="Hola title"
        android:textSize="@dimen/card_title_text_size" />

    <TextView
        android:id="@+id/txt_widget_product_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/card_title_margin_start"
        android:layout_marginEnd="@dimen/card_title_margin_end"
        android:text="@string/text_description_label"
        android:textColor="@color/colorPrimaryText"
        android:textSize="@dimen/card_title_text_size" />

</LinearLayout>